"use client";
import {FormEvent,useState} from "react";
import {supabase} from "../supabase";
export default function LoginPage(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [working,setWorking]=useState(false);async function submit(e:FormEvent){e.preventDefault();setWorking(true);setError("");const {error:authError}=await supabase.auth.signInWithPassword({email,password});if(authError){setError(authError.message==="Invalid login credentials"?"The email or password is incorrect.":authError.message);setWorking(false)}}return <main className="login-page"><section className="login-brand"><div className="login-mark">NKH</div><p>N K HOTELS</p><h1>Performance clarity<br/>for every hotel.</h1><span>Hotel performance and revenue operations for the Lavendish Leisure portfolio.</span><div className="login-metric"><b>10</b><small>HOTELS IN ONE PORTFOLIO</small></div></section><section className="login-panel"><form onSubmit={submit}><div className="login-logo">NKH</div><p className="eyebrow">PERFORMANCE HUB</p><h2>Welcome back</h2><span className="login-help">Sign in to your hotel operations workspace.</span><label><span>Email address</span><input required type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="name@company.com"/></label><label><span>Password</span><input required type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Your app password"/></label>{error&&<div className="login-error">{error}</div>}<button disabled={working}>{working?"Signing in…":"Sign in"}</button><small className="login-note">Securely developed and operated by N K Hotels.</small></form></section></main>}
