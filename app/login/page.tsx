"use client";
import {FormEvent,useState} from "react";
import {supabase} from "../supabase";
export default function LoginPage(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [working,setWorking]=useState(false);async function submit(e:FormEvent){e.preventDefault();setWorking(true);setError("");const {error:authError}=await supabase.auth.signInWithPassword({email,password});if(authError){setError(authError.message==="Invalid login credentials"?"The email or password is incorrect.":authError.message);setWorking(false)}}return <main className="login-page"><section className="login-brand"><div className="login-mark">NK</div><p>N K HOTELS</p><h1>Occupancy clarity<br/>for every hotel.</h1><span>Live, read-only intelligence for Lavendish hotel leadership.</span><div className="login-metric"><b>10</b><small>HOTELS IN ONE VIEW</small></div></section><section className="login-panel"><form onSubmit={submit}><div className="login-logo">LH</div><p className="eyebrow">MANAGEMENT ACCESS</p><h2>Welcome back</h2><span className="login-help">Sign in with your occupancy app account.</span><label><span>Email address</span><input required type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="name@company.com"/></label><label><span>Password</span><input required type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Your app password"/></label>{error&&<div className="login-error">{error}</div>}<button disabled={working}>{working?"Signing in…":"Sign in"}</button><small className="login-note">Your Google password is never used here.</small></form></section></main>}
