"use client";
import {FormEvent,useEffect,useState} from "react";
import {useAuth} from "../../auth-provider";
const hotelOptions=[{code:"MLR",name:"Miridiya Lake Resort"},{code:"GTL",name:"Grand Tamarind Lake"},{code:"LOH",name:"Lavendish Okrin Hotel"},{code:"LWS",name:"Lavendish Wild Safari"},{code:"LWW",name:"Lavendish Wild Wilpattu"},{code:"LCR",name:"Lavendish Country Resort"},{code:"LLG",name:"Lavendish Lake Giritale"},{code:"LHK",name:"Lavendish Hills Kandy"},{code:"TLK",name:"Tamarind Lifestyle - Kataragama"},{code:"LBU",name:"Lavendish Beach Unawatuna"}];
type Role="MASTER_ADMIN"|"HEAD_OFFICE"|"GM"|"VIEWER";type User={id:string;name:string;email:string;password:string;role:Role;hotels:string[];active:boolean};
const blank:User={id:"",name:"",email:"",password:"",role:"GM",hotels:[],active:true};const roleLabel:Record<Role,string>={MASTER_ADMIN:"Master Admin",HEAD_OFFICE:"Head Office",GM:"General Manager",VIEWER:"Viewer"};
export function SettingsUsersClient(){const {access,session}=useAuth();const [users,setUsers]=useState<User[]>([]),[editing,setEditing]=useState<User|null>(null),[notice,setNotice]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false);async function api(options?:RequestInit){if(!session)throw new Error("Session expired");const response=await fetch("/api/admin/users",{...options,headers:{"Content-Type":"application/json",Authorization:`Bearer ${session.access_token}`,...options?.headers}}),data=await response.json();if(!response.ok)throw new Error(data.error??"Request failed");return data}useEffect(()=>{if(!session||access?.role!=="MASTER_ADMIN")return;let active=true;setLoading(true);api().then(data=>{if(active)setUsers(data.users.map((user:Omit<User,"password">)=>({...user,password:""})))}).catch(err=>{if(active)setError(err.message)}).finally(()=>{if(active)setLoading(false)});return()=>{active=false}},[session,access?.role]);function toggleHotel(code:string){setEditing(user=>user?{...user,hotels:user.hotels.includes(code)?user.hotels.filter(x=>x!==code):[...user.hotels.filter(x=>x!=="ALL"),code]}:user)}async function save(event:FormEvent){event.preventDefault();if(!editing)return;setSaving(true);setError("");try{const data=await api({method:editing.id?"PATCH":"POST",body:JSON.stringify(editing)}),next={...data.user,password:""} as User;setUsers(current=>editing.id?current.map(user=>user.id===next.id?next:user):[...current,next].sort((a,b)=>a.name.localeCompare(b.name)));setNotice(editing.id?`${next.name}'s account was updated.`:`${next.name} can now sign in.`);setEditing(null);window.setTimeout(()=>setNotice(""),5000)}catch(err){setError(err instanceof Error?err.message:"Could not save user")}finally{setSaving(false)}}return <div className="intel-page settings-users-page"><section className="intel-intro"><div><h2>User control</h2><p>Create accounts, assign roles and choose exactly which Lavendish hotels each person can access.</p></div><button className="intel-primary" onClick={()=>{setError("");setEditing({...blank})}}>+ Create user</button></section><section className="admin-stats"><article><span>Total users</span><b>{users.length}</b><small>Supabase accounts</small></article><article><span>Active users</span><b>{users.filter(u=>u.active).length}</b><small>Allowed to sign in</small></article><article><span>Full portfolio</span><b>{users.filter(u=>u.hotels.includes("ALL")).length}</b><small>All 10 hotels</small></article><article><span>Authentication</span><b className="status-word">Live</b><small>Master controlled</small></article></section><section className="admin-panel"><div className="admin-panel-head"><div><h2>Authorized users</h2><p>Passwords are encrypted and never displayed after creation.</p></div></div>{error&&!editing&&<div className="admin-inline-error">{error}</div>}{loading?<div className="admin-empty">Loading authorized users…</div>:<div className="users-table"><div className="users-head"><span>User</span><span>Role</span><span>Hotel access</span><span>Status</span><span/></div>{users.map(user=><div className="users-row" key={user.id}><div className="user-name"><span>{user.name.split(" ").map(x=>x[0]).join("").slice(0,2)}</span><div><b>{user.name}</b><small>{user.email}</small></div></div><span className={`role-pill ${user.role.toLowerCase()}`}>{roleLabel[user.role]}</span><div className="access-summary">{user.hotels.includes("ALL")?<b>All 10 hotels</b>:<><b>{user.hotels.length} hotels</b><small>{user.hotels.join(" · ")||"No access"}</small></>}</div><span className={`profile-status ${user.active?"active":"inactive"}`}>{user.active?"Active":"Inactive"}</span><button className="edit-button" onClick={()=>{setError("");setEditing({...user,password:"",hotels:[...user.hotels]})}}>Edit</button></div>)}</div>}</section><section className="architecture-note"><span>✓</span><div><b>Secure account control</b><p>Only the Master can enter this area. Existing occupancy and intelligence permissions remain enforced by the server.</p></div></section>{editing&&<div className="modal-backdrop" onMouseDown={event=>{if(event.currentTarget===event.target&&!saving)setEditing(null)}}><form className="profile-modal user-modal" onSubmit={save}><div className="modal-head"><div><p>USER ACCOUNT</p><h2>{editing.id?"Edit user":"Create user"}</h2></div><button type="button" disabled={saving} onClick={()=>setEditing(null)}>×</button></div>{error&&<div className="admin-inline-error modal-error">{error}</div>}<div className="form-grid"><label><span>Full name *</span><input required value={editing.name} onChange={e=>setEditing({...editing,name:e.target.value})}/></label><label><span>Email address *</span><input required type="email" value={editing.email} onChange={e=>setEditing({...editing,email:e.target.value})}/></label><label className="wide"><span>{editing.id?"New password (optional)":"Initial password *"}</span><input required={!editing.id} minLength={8} type="password" autoComplete="new-password" value={editing.password} onChange={e=>setEditing({...editing,password:e.target.value})}/><small>{editing.id?"Leave blank to keep the current password.":"Minimum 8 characters."}</small></label><label><span>Role *</span><select value={editing.role} onChange={e=>setEditing({...editing,role:e.target.value as Role})}><option value="MASTER_ADMIN">Master Admin</option><option value="HEAD_OFFICE">Head Office</option><option value="GM">General Manager</option><option value="VIEWER">Viewer</option></select></label><label><span>Account status</span><select value={editing.active?"Active":"Inactive"} onChange={e=>setEditing({...editing,active:e.target.value==="Active"})}><option>Active</option><option>Inactive</option></select></label></div><div className="hotel-access-title"><div><b>Hotel access *</b><small>Select one, several, or all hotels.</small></div><button type="button" className={editing.hotels.includes("ALL")?"selected":""} onClick={()=>setEditing({...editing,hotels:["ALL"]})}>✓ All 10 hotels</button></div><div className="hotel-check-grid">{hotelOptions.map(h=><label className={editing.hotels.includes("ALL")||editing.hotels.includes(h.code)?"selected":""} key={h.code}><input type="checkbox" checked={editing.hotels.includes("ALL")||editing.hotels.includes(h.code)} disabled={editing.hotels.includes("ALL")} onChange={()=>toggleHotel(h.code)}/><span>{h.code}</span><b>{h.name}</b></label>)}</div><div className="modal-actions"><button type="button" disabled={saving} onClick={()=>setEditing(null)}>Cancel</button><button className="save-profile" type="submit" disabled={saving||!editing.hotels.length}>{saving?"Saving securely…":editing.id?"Update user":"Create user & login"}</button></div></form></div>}{notice&&<div className="admin-toast">✓ {notice}</div>}</div>}
